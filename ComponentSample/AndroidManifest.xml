<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.componentsample"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.componentsample.NewsSimpleLaunchActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.example.componentsample.NewsSimpleService"
            android:enabled="true" >
        </service>

        <!--
        <receiver
            android:name=".NewsSimpleLaunchActivity$NewsBroadcastReceiver"
            android:enabled="true" >
            <intent-filter>
                <action android:name="kyw.intent.action.VIEW_NEWS" />
            </intent-filter>
        </receiver>
        -->
    </application>

</manifest>